// This layout is for the marketing homepage only
// It doesn't include header/footer to avoid duplication
export default function MarketingHomeLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}